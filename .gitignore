# Personal project notes and strategy — not for the public repo.
# ROADMAP.md names competitors' weaknesses, where this app is behind, and
# unmade pricing decisions. That is planning material, not documentation.
CLAUDE.md
ROADMAP.md

# Xcode
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout
DerivedData/
.build/
.swiftpm/

# macOS
.DS_Store

# CocoaPods
# `Pods/` is ~1.2 GB — regenerate it with `pod install`.
# Podfile and Podfile.lock ARE tracked so installs are reproducible.
Pods/
